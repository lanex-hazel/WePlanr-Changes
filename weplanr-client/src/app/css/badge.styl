badge($color,$bg-color)
  padding: 4px 9px
  background-color: $bg-color
  color: $color !important
  border-radius 60px
  font-size: 11px
  font-weight bold


.badge
  badge($white,$gray)

.badge-success
  badge($white,#7dcf6a)

.badge-danger
  badge($white,$danger)
